# evoluum-api-locality

Instructions for running the code

- In the root of the project, execute
- yarn (installation of dependencies)
- yarn dev (run the application)

Endpoints

- /json
- /csv
- /city with query parameter "nomeCidade"
